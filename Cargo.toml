[package]
name = "io_core"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"
exclude = ["todo.md"]

[dependencies]
memchr = { version = "2.8.0", default-features = false }

[features]
default = []
alloc = []
std = ["alloc"]

# use nightly features
nightly = []
